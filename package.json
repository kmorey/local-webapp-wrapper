{
  "name": "local-webapp-wrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Configurable Electron webapp windows that route external links to the system browser.",
  "main": "src/main.js",
  "bin": {
    "local-webapp-wrapper": "bin/local-webapp-wrapper"
  },
  "scripts": {
    "check": "node --check src/main.js && node --check src/preload.js && node --check scripts/install-desktop.js",
    "install-user": "bash scripts/install-user.sh",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^42.0.1"
  }
}
